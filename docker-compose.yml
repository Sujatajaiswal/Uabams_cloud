services:
  uabams-cloud:
    build: .
    container_name: uabams_app
    ports:
      - "8000:8000"
    environment:
      DATABASE_TYPE: postgres
      DATABASE_URL: postgresql://uabams_db_user:uUBZbdHXte5FYIFph5bDmx1szS0Hon6w@dpg-d9fkl1v7f7vs73bc5hk0-a.singapore-postgres.render.com/uabams_db?sslmode=require
      JWT_SECRET: local-docker-change-me
      JWT_ALGORITHM: HS256
      ADMIN_RESET_KEY: local-admin-key
      OPERATOR_USERNAME: admin
      OPERATOR_PASSWORD: admin123
    restart: always
